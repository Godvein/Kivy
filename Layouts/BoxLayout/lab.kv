BoxLayoutWidget:

<BoxLayoutWidget>:
    orientation: "vertical"
    Button:
        text: "A"
        size_hint: .8, .5
        # x, center_x, right
        # y, center_y, top
        pos_hint: {"x": .5}

    Button:
        text: "B"
        size_hint: None, None
        size: "200dp", "200dp"